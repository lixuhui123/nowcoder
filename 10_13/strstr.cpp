#include <stdio.h>
#include <string.h>
#include <stdlib.h>
int mainvdfesgxsawaw()
{
	char str[] = "This is a simple string";
	char * pch;
	pch = strstr(str, "impl");
	strncpy(pch, "sample", 2);
	puts(pch);
	system("pause");
	return 0;
}
